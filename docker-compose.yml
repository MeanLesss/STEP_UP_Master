version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: step_up_app
    container_name: step_up_app
    restart: unless-stopped
    tty: true
    environment:
      SERVICE_NAME: app
      SERVICE_TAGS: dev
    working_dir: /var/www/html/STEP_UP_Master
    volumes:
      - ./:/var/www/html/STEP_UP_Master
      - ./php/local.ini:/usr/local/etc/php/conf.d/local.ini
    networks:
      - laravel

  node:
    image: node:14.15.1
    container_name: node
    volumes:
      - ./:/var/www
    working_dir: /var/www
    command: bash -c "yarn install && yarn run dev"

  nginx:
    image: nginx:latest
    container_name: docker-nginx
    ports:
      - "168:80"
    volumes:
      - ./:/usr/share/nginx/html/STEP_UP_Master
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - laravel

networks:
  laravel:
    driver: bridge
